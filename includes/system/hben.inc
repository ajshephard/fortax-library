@:fortaxdef(bool,    doHBen,                   "Calculate housing benefit",       null, label = bool)
@:fortaxdef(double,  taper,                    "Taper rate",                      rate)
@:fortaxdef(double,  MinAmt,                   "Minimum amount",                  minamount)
@:fortaxdef(bool,    doUnderOccCharge,         "Under occupancy charge",          null, label = bool)
@:fortaxdef(bool,    doUnderOccChargeScotland, "Under occupancy charge Scotland", null, label = bool)
@:fortaxdef(bool,    doUnderOccChargeNI,       "Under occupancy charge NI",       null, label = bool)
@:fortaxdef(integer, numUnderOccBands,         "Under occupancy bands",           range)
@:fortaxdef(double,  underOccRates,            "Under occupancy rates",           rate, dim = maxUnderOccBands, dimvar = numUnderOccBands)
@:fortaxdef(bool,    doLHA,                    "Local housing allowance",         null)
@:fortaxdef(integer, LHASharedAccAge,          "Shared accommodation age",        range)
@:fortaxdef(integer, numLHABands,              "Number LHA bands",                range)
@:fortaxdef(double,  LHARates,                 "LHA Rates",                       rate, dim = maxLHABands, dimvar = numLHABands)

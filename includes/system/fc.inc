@:header(fc)
#:if definitions > 0
@:bool(dofamcred, null)
@:integer(NumAgeRng, range)
@:integer(MaxAgeCC, range)
@:integer(WFTCMaxAgeCC, range)
@:double(adult, amount)
@:double(ftprem, amount)
@:double(hours1, range)  
@:double(hours2, range)  
@:double(thres, amount)  
@:double(taper, rate)   
@:double(MaintDisreg, amount)
@:double(MaxCC1, amount)
@:double(MaxCC2, amount)  
@:double(WFTCMaxCC1, amount)
@:double(WFTCMaxCC2, amount)
@:double(WFTCPropCC, rate)
@:double(MinAmt, minamount)
@:integerarray(kidagel, range, MaxNumAgeRng, NumAgeRng)
@:integerarray(kidageu, range, MaxNumAgeRng, NumAgeRng)
@:doublearray(kidcred, amount, MaxNumAgeRng, NumAgeRng)
#:endif
@:footer(fc)

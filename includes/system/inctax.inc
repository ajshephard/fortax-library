@:header(inctax)
#:if definitions > 0
@:integer(numbands, range)
@:double(pa, amount)
@:bool(doPATaper, null)
@:bool(disablePATaperRounding, null)
@:double(paTaperThresh, amount)
@:double(paTaperRate, rate)
@:double(mma, amount)
@:double(ctc, amount)
@:double(ctcyng, amount)
@:double(mmarate, rate)
@:double(ctctaper, rate)
@:double(c4rebate, rate)
@:doublearray(bands, amount, MaxIncTaxBands, numbands)
@:doublearray(rates, rate, MaxIncTaxBands, numbands)
#:endif
@:footer(inctax)

@:fortaxdef(bool,    couple,      "Married/cohabiting",       null, label = bool)
@:fortaxdef(bool,    married,     "Married",                  null, label = bool)
@:fortaxdef(double,  ccexp,       "Childcare expenditure",    amount)
@:fortaxdef(double,  maint,       "Maintenance income",       amount)
@:fortaxdef(integer, nkids,       "Number of children",       null)
@:fortaxdef(integer, kidage,      "Age of children",          null, dim = maxKids, dimvar = nkids)
@:fortaxdef(integer, kidsex,      "Sex of children",          null, dim = maxKids, dimvar = nkids, label = sex)
@:fortaxdef(integer, nothads,     "Number of other adults",   null)
@:fortaxdef(integer, tenure,      "Housing tenure",           null, value = ${fortax_value_lab("tenure", "own_outright")}$, label = tenure)
@:fortaxdef(double,  rent,        "Housing rent",             amount)
@:fortaxdef(double,  rentcap,     "Housing rent cap",         amount)
@:fortaxdef(integer, bedrooms,    "Number of bedrooms",       range)
@:fortaxdef(integer, region,      "Region",                   null, value = ${fortax_value_lab("region", "north_east")}$, label = region)
@:fortaxdef(integer, ctband,      "Council tax band",         null, value = ${fortax_value_lab("ctax", "bandd")}$, label = ctax)
@:fortaxdef(double,  banddratio,  "Council tax band-D ratio", null, value = 1.0)
@:fortaxdef(integer, intdate,     "Interview date",           null, value = 19900101)
@:fortaxdef(integer, famtype,     "Family type",              null, value = ${fortax_value_lab("famtype", "single_nokids")}$, label = famtype, ignore = True)
@:fortaxdef(integer, yngkid,      "Youngest child",           null, ignore = True)
@:fortaxdef(integer, kidagedist,  "Child age dist.",          null, dim = 20, lowerdim = -1, ignore = True)
@:fortaxdef(integer, kidagedist0, "Child age dist. (male)",   null, dim = 20, lowerdim = -1, ignore = True)
@:fortaxdef(integer, kidagedist1, "Child age dist. (female)", null, dim = 20, lowerdim = -1, ignore = True)
